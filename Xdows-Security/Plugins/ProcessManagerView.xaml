<UserControl
    x:Class="Xdows_Security.Views.ProcessManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Xdows_Security.Views"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <CommandBar Grid.Row="0">
            <CommandBar.Content>
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="进程管理器"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Margin="12,16,0,0"
                               FontSize="16"/>
                    <ComboBox x:Name="SortCombo"
                              Grid.Column="1"
                              Margin="16,12,0,0"
                              SelectionChanged="SortCombo_SelectionChanged"
                              Width="120">
                        <ComboBoxItem Content="名称" Tag="Name"/>
                        <ComboBoxItem Content="PID" Tag="Id"/>
                        <ComboBoxItem Content="内存" Tag="Memory"/>
                        <ComboBoxItem Content="线程" Tag="Threads"/>
                        <ComboBoxItem Content="句柄" Tag="Handles"/>
                    </ComboBox>
                    <TextBox x:Name="SearchBox"
                             Grid.Column="2"
                             PlaceholderText="键入要搜索的内容..."
                             HorizontalAlignment="Center"
                             Width="Auto"
                             MinWidth="240"
                             Margin="16,12,0,0"
                             TextChanged="SearchBox_TextChanged"/>
                </Grid>
            </CommandBar.Content>
            <AppBarButton Icon="Refresh" Label="刷新列表" Click="Refresh_Click"/>
            <AppBarButton Icon="Cancel" Label="结束进程" Click="Kill_Click"/>
            <AppBarButton Label="挂起" Click="Suspend_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE769;"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="恢复" Click="Resume_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE768;"/>
                </AppBarButton.Icon>
            </AppBarButton>

            <AppBarButton Label="详细信息" Click="ShowProcessDetail_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE946;"/>
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
        <ListView x:Name="ProcessList" Grid.Row="1" SelectionMode="Single" DoubleTapped="ShowProcessDetail_Click">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ProcessInfoEx">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Name}" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind Id}" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{x:Bind ThreadCount}" FontSize="14" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock Grid.Column="3" Text="{x:Bind HandleCount}" FontSize="14" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock Grid.Column="4" Text="{x:Bind Memory}" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        <Grid.ContextFlyout>
                            <MenuFlyout x:Name="ItemMenuFlyout">
                                <MenuFlyoutItem Text="刷新列表" Click="Refresh_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE72C;"  />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutSeparator/>
                                <MenuFlyoutItem Text="详细信息" Click="ShowProcessDetail_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE946;"/>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>

                                <MenuFlyoutItem Text="挂起进程" Click="Suspend_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE769;"/>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="恢复进程" Click="Resume_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE768;"/>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutSeparator/>
                                <MenuFlyoutItem Text="终止进程" Click="Kill_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xF140;"/>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Grid.ContextFlyout>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
