<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Xdows_Security.SecurityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Xdows_Security"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Storyboard x:Name="RadarScanStoryboard">
            <DoubleAnimation Storyboard.TargetName="RadarScanLine"
                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                         From="0" To="360" Duration="0:0:3" RepeatBehavior="Forever"/>
        </Storyboard>

        <Storyboard x:Name="RadarLineAppearStoryboard">
            <DoubleAnimation Storyboard.TargetName="RadarScanLine"
                     Storyboard.TargetProperty="Opacity"
                     From="0" To="0.8" Duration="0:0:0.3"/>
        </Storyboard>

        <Storyboard x:Name="RadarLineDisappearStoryboard">
            <DoubleAnimation Storyboard.TargetName="RadarScanLine"
                         Storyboard.TargetProperty="Opacity"
                         From="0.8" To="0" Duration="0:0:0.2"/>
        </Storyboard>
    </Page.Resources>
    <ScrollViewer>
        <Grid Margin="64,16,64,0" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 扫描区域状态显示 -->
            <Border BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultSolid}" Grid.Row="0" Grid.ColumnSpan="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="扫描状态" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon x:Name="ScanAreaIcon" Grid.Column="0" Glyph="&#xE721;" FontSize="24" Margin="0,0,12,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock x:Name="CurrentScanAreaText" Text="准备开始扫描" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock x:Name="ScanProgressDetailText" Text="等待用户选择扫描类型" FontSize="12" Opacity="0.7"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
            <Border BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultSolid}" Grid.Row="3" Grid.ColumnSpan="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,8">
                <StackPanel>
                    <Grid Margin="0,0,0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="扫描进度" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" x:Name="ProgressPercentText" Text="0%" FontSize="14" FontWeight="Bold"/>
                    </Grid>

                    <!-- 雷达扫描区域 -->
                    <Grid Height="120" Margin="0,8,0,8">
                        <!-- 雷达背景圆圈 -->
                        <Ellipse Width="100" Height="100" Stroke="{ThemeResource AccentTextFillColorTertiaryBrush}" StrokeThickness="2" Opacity="0.3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Ellipse Width="80" Height="80" Stroke="{ThemeResource AccentTextFillColorTertiaryBrush}" StrokeThickness="1" Opacity="0.2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Ellipse Width="60" Height="60" Stroke="{ThemeResource AccentTextFillColorTertiaryBrush}" StrokeThickness="1" Opacity="0.2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Ellipse Width="40" Height="40" Stroke="{ThemeResource AccentTextFillColorTertiaryBrush}" StrokeThickness="1" Opacity="0.2" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <!-- 雷达扫描线 -->
                        <Line x:Name="RadarScanLine" X1="30" Y1="130" X2="0" Y2="0" Stroke="{ThemeResource AccentTextFillColorTertiaryBrush}" StrokeThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0">
                            <Line.RenderTransform>
                                <RotateTransform CenterX="15" CenterY="65"/>
                            </Line.RenderTransform>
                        </Line>

                        <!-- 雷达脉冲效果 -->
                        <Ellipse x:Name="RadarPulse" Width="20" Height="20" Fill="{ThemeResource AccentTextFillColorTertiaryBrush}" Opacity="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Ellipse.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform CenterX="10" CenterY="10"/>
                                    <RotateTransform CenterX="10" CenterY="10"/>
                                </TransformGroup>
                            </Ellipse.RenderTransform>
                        </Ellipse>

                        <!-- 中心点 -->
                        <Ellipse Width="8" Height="8" Fill="{ThemeResource AccentTextFillColorTertiaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- 进度条 -->
                    <ProgressBar x:Name="ScanProgress" Height="8" CornerRadius="4" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Foreground="{ThemeResource AccentTextFillColorTertiaryBrush}"/>

                    <!-- 统计信息 -->
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE8B7;" FontSize="24" Margin="0,0,0,4" Foreground="{ThemeResource AccentTextFillColorTertiaryBrush}"/>
                            <TextBlock x:Name="FilesScannedText" Text="0 个文件" FontSize="12" TextAlignment="Center" Foreground="{ThemeResource AccentTextFillColorTertiaryBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE7C9;" FontSize="24" Margin="0,0,0,4" Foreground="{ThemeResource AccentTextFillColorTertiaryBrush}"/>
                            <TextBlock x:Name="FilesSafeText" Text="0 个安全" FontSize="12" TextAlignment="Center" Foreground="{ThemeResource AccentTextFillColorTertiaryBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE783;" FontSize="24" Margin="0,0,0,4" Foreground="{ThemeResource AccentTextFillColorTertiaryBrush}"/>
                            <TextBlock x:Name="ThreatsFoundText" Text="0 个威胁" FontSize="12" TextAlignment="Center" Foreground="{ThemeResource AccentTextFillColorTertiaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 扫描路径和选择按钮 -->
            <TextBlock Name="PathText"
           Grid.Row="4" Grid.Column="0"
           Text="选择扫描文件"
           FontSize="16"
           VerticalAlignment="Center"
           Margin="0,0,8,16"/>

            <StackPanel Grid.Row="4" Grid.Column="1"
            Orientation="Horizontal"
            Spacing="8"
            VerticalAlignment="Center"
            Margin="0,0,0,16">
                <Button x:Name="BackToVirusListButton" Click="OnBackToVirusListClick" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal" Tag="Visible" Spacing="8">
                        <FontIcon x:Name="BackToVirusListButtonIcon" Glyph="&#xE890;"/>
                        <TextBlock x:Name="BackToVirusListButtonText" Text="查看列表"/>
                    </StackPanel>
                </Button>

                <Button x:Name="PauseScanButton" Click="OnPauseScanClick" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xF5E7;"/>
                        <TextBlock Text="暂停扫描"/>
                    </StackPanel>
                </Button>

                <Button x:Name="ResumeScanButton" Click="OnResumeScanClick" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE768;"/>
                        <TextBlock Text="继续扫描"/>
                    </StackPanel>
                </Button>

                <DropDownButton Name="ScanButton">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE8B7;" FontSize="16"/>
                        <TextBlock Text="开始扫描"/>
                    </StackPanel>
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Text="快速扫描" Tag="Quick" Click="OnScanMenuClick">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE945;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="全盘扫描" Tag="Full" Click="OnScanMenuClick">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xEDA2;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem Text="文件扫描" Tag="File" Click="OnScanMenuClick">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE8A5;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="目录扫描" Tag="Folder" Click="OnScanMenuClick">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE8B7;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem Text="更多扫描" Tag="More" Click="OnScanMenuClick">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE944;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </StackPanel>

            <!-- 状态文本 -->
            <TextBlock Name="StatusText" Grid.Row="5" Margin="0,8,0,0" FontSize="14" TextWrapping="Wrap" Grid.ColumnSpan="2"/>

            <!-- 病毒列表 -->
            <ListView Name="VirusList" Grid.Row="6" Margin="-28,10,0,8" Visibility="Collapsed" DoubleTapped="VirusList_DoubleTapped" Grid.ColumnSpan="2">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="12,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" MinWidth="300"/>
                                <ColumnDefinition Width="*"   MinWidth="150"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding FilePath}" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Column="1" Text="{Binding VirusName}"/>

                            <Grid.ContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="详细信息" Click="OnDetailClick" Tag="{Binding}">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE946;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutSeparator/>
                                    <MenuFlyoutItem Text="删除文件" Click="OnDeleteClick" Tag="{Binding}">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE74D;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Grid.ContextFlyout>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </ScrollViewer>
</Page>